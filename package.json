{
  "name": "bardo-discord",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Bardo publica previews de Markdown en Discord y abre el documento completo como una Discord Activity serverless sobre Cloudflare Workers.",
  "scripts": {
    "build:activity": "node scripts/build-activity.js",
    "build": "npm run build:activity",
    "predeploy": "npm run build:activity",
    "dev": "npm run build:activity && wrangler dev",
    "deploy": "npm run build:activity && wrangler deploy",
    "register": "node --env-file=.env scripts/register-commands.js",
    "test": "node --test",
    "check": "node scripts/build-activity.js && node --check src/worker.js && node --check src/components.js && node --check src/pagination.js && node --check src/db.js && node --check activity/app.js && node --check activity/vendor/discord-sdk.js && node --check scripts/register-commands.js && node --check scripts/build-activity.js"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.5.0",
    "discord-interactions": "^4.1.2",
    "discord.js": "^14.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "wrangler": "^4.124.0"
  }
}
