{
  "name": "bardo-discord",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Bardo importa Markdown, TXT, PDF y DOCX y los abre con un lector unificado dentro de Discord.",
  "scripts": {
    "build:activity": "node scripts/build-activity.js",
    "build": "npm run build:activity",
    "predeploy": "npm run build:activity",
    "dev": "npm run build:activity && wrangler dev",
    "deploy": "npm run build:activity && wrangler deploy",
    "register": "node --env-file=.env scripts/register-commands.js",
    "test": "npm run build:activity && node --test",
    "check": "node scripts/build-activity.js && node --check src/worker.js && node --check src/components.js && node --check src/document-id.js && node --check src/export-format.js && node --check src/import-format.js && node --check src/pagination.js && node --check src/db.js && node --check src/activity/app.js && node --check scripts/register-commands.js && node --check scripts/build-activity.js"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.5.0",
    "discord-interactions": "^4.1.2",
    "discord.js": "^14.18.0",
    "mammoth": "^1.12.1",
    "turndown": "^7.2.4",
    "turndown-plugin-gfm": "^1.0.2",
    "unpdf": "^1.8.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "wrangler": "^4.124.0"
  }
}
