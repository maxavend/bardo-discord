{
  "name": "bardo-discord",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Bardo publica previews de Markdown en Discord y abre el documento completo como una Discord Activity serverless sobre Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register": "node --env-file=.env scripts/register-commands.js",
    "test": "node --test",
    "check": "node --check src/worker.js && node --check src/components.js && node --check src/pagination.js && node --check src/db.js && node --check activity/app.js && node --check scripts/register-commands.js"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "discord-interactions": "^4.1.2",
    "discord.js": "^14.18.0"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  }
}
